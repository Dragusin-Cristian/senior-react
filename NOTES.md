# NOTES FOR EACH SECTION

## Functional Programming (just 3 other design pattern examples)

- recursive components: simply a component calling recursively itself until the return condition is met
- compositions: simply a component returning a base component with more specific props
- parital components: a composition but using HOC